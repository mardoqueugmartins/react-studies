const Events = () => {
  return (
    <div>
      <div>
        <button onClick={() => console.log("Testando evento de clique.")}>
          Clique aqui.
        </button>
      </div>
    </div>
  );
};

export default Events;
